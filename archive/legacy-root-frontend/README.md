# Legacy Root Frontend

This folder is the archive marker for the obsolete root-level frontend scaffold that previously lived under `c:\FIFA WC\src`.

Current active frontend runtime:
- `platform/frontend`

Archived/legacy root frontend surface:
- `src/`
- `vite.config.ts`
- `tsconfig.json`
- root-level frontend API wrappers, pages, layout, hooks, store, and mocks

Do not extend this surface. All new frontend work should stay inside `platform/frontend`.
